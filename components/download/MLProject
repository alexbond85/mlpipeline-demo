name: download_training_data


entry_points:
  main:
    parameters:

      data_path:
        description: Name of the input artifact in W&B
        type: string

      output_artifact:
        description: Name for the output artifact
        type: string

    command: "python run.py --data_path {data_path} --output_artifact {output_artifact}"
