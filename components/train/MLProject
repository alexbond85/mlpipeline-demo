name: train_model


entry_points:
  main:
    parameters:

      input_data:
        description: Name of the input artifact in W&B
        type: string

      output_artifact:
        description: Name for the output artifact
        type: string

    command: "python run.py --input_data {input_data} --output_artifact {output_artifact}"
